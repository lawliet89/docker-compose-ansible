---
code_source: 'local'
get_local_commit: True
git_repo_version: 'master'
docker_compose_files:
  - docker-compose.yml
remove_volumes: no
pull_images: True
build_images: True
docker_become: {}
  become: false
  # become_user: ""
  # become_method: ""
  # become_flags: ""
